# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'MyGuide Dashboards'
    orgId: 1
    folder: 'MyGuide'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards